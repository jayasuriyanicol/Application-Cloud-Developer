'''
Si consideri un sistema software per la gestione dei pagamenti che utilizza la seguente interfaccia:

public interface PaymentProcessor {
    void pay(double amount);
}
'''
package eserciziLezione12;

public interface PaymentProcessor {
	
	void pay (double amount);

}
