package com.spring.cc.dto;

// * Creating a record for returning information request data at controller stage
public record CCRichiestaDTO(Double saldoIniziale, Integer idIntestatario, 	Integer idCointestatario) {}
